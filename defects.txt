При выполнении работы найдены и исправлены следующие ошибки:

Файл index.html.
1. Лишний пробел в подвале страницы. Сейчас "202 4", а должно быть "2024".
2. Два "Ы" вместо одного в заголовке "Основные"
3. Отсуствует "С" в слове инструменты.
4. Отсуствует "С" в слове тестирование. 

Файл cmd.html.
2. dir — Отображение файлов и папок в текущем каталоге
3. cd — Служит для перемещения между папками
4. mkdir — Создать папку

Файл mac.html.
1. mv — расфшифровывается как"Move", что переводится как "переместить", "передвинуть". Можем переместить файл из одной папки в другую, или переименовать файл
2. mkdir — расшифровывается как "Make directory", что переводится как "создать папку".

Файл git.html
1. git init — создание репозитория
2. git add — добавляет содержимое рабочего каталога в индекс (staging area) для последующего коммита.
3. git commit — берёт все данные, добавленные в индекс с помощью git add, и сохраняет их слепок во внутренней базе данных, а затем сдвигает указатель текущей ветки на этот слепок.